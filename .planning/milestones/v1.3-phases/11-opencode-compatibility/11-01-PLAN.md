---
phase: 11-opencode-compatibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/pr-reviewer.md
  - agents/pr-fixer.md
  - commands/pr-review/review.md
  - commands/pr-review/fix.md
  - commands/pr-review/setup.md
autonomous: true
requirements:
  - RTCOMPAT-01
  - RTCOMPAT-02

must_haves:
  truths:
    - "Every agent and command file contains a runtime-compat HTML comment block documenting Claude Code and OpenCode behavior"
    - "All tool names in frontmatter are confirmed PascalCase (no changes needed, audit documented in compat blocks)"
    - "A user reading any agent/command file can see inline what works, what degrades, and what fallback exists on each runtime"
  artifacts:
    - path: "agents/pr-reviewer.md"
      provides: "Compat block for review agent"
      contains: "<!-- runtime-compat"
    - path: "agents/pr-fixer.md"
      provides: "Compat block for fix agent"
      contains: "<!-- runtime-compat"
    - path: "commands/pr-review/review.md"
      provides: "Compat block for review command"
      contains: "<!-- runtime-compat"
    - path: "commands/pr-review/fix.md"
      provides: "Compat block for fix command"
      contains: "<!-- runtime-compat"
    - path: "commands/pr-review/setup.md"
      provides: "Compat block for setup command"
      contains: "<!-- runtime-compat"
  key_links:
    - from: "compat blocks"
      to: "YAML frontmatter"
      via: "placement after closing --- per D-02"
      pattern: "---\\n\\n<!-- runtime-compat"
---

<objective>
Add inline runtime compatibility documentation to all agent and command files.

Purpose: Users installing on OpenCode can see exactly what works, what degrades, and what fallback behavior exists — without opening external docs. Also confirms tool name audit results (all PascalCase, zero mismatches) per RTCOMPAT-01.

Output: 5 files modified with HTML comment compat blocks placed after frontmatter, before first XML tag.
</objective>

<execution_context>
@E:/Proyectos/pr-review-agent/.claude/get-shit-done/workflows/execute-plan.md
@E:/Proyectos/pr-review-agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-opencode-compatibility/11-CONTEXT.md
@.planning/phases/11-opencode-compatibility/11-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add compat blocks to agent files</name>
  <files>agents/pr-reviewer.md, agents/pr-fixer.md</files>
  <read_first>agents/pr-reviewer.md, agents/pr-fixer.md, .planning/phases/11-opencode-compatibility/11-RESEARCH.md</read_first>
  <action>
Insert an HTML comment compat block in each agent file per D-01 and D-02.

**Placement (D-02):** After the closing `---` of YAML frontmatter, add one blank line, then the comment block, then one blank line before the existing `<role>` tag. The `---` line, blank line, comment block, blank line, `<role>` must appear in that exact order.

**agents/pr-reviewer.md** — Insert this block between line 6 (`---`) and line 8 (`<role>`):

```
<!-- runtime-compat
runtime: claude-code
  status: full
  notes: All tools native. Interactive prompts use bash readline (not AskUserQuestion).
runtime: opencode
  status: partial
  degraded: tools: field deprecated -> permissions managed via opencode.json
  notes: AskUserQuestion not invoked; agents use bash readline for all interactive prompts.
         allowed-tools and argument-hint on command files are silently ignored.
         PascalCase tool names in tools: field may be deprecated; opencode uses lowercase internally.
-->
```

**agents/pr-fixer.md** — Insert this block between line 6 (`---`) and line 8 (`<role>`):

```
<!-- runtime-compat
runtime: claude-code
  status: full
  notes: All tools native. No interactive prompts in this agent.
runtime: opencode
  status: partial
  degraded: tools: field deprecated -> permissions managed via opencode.json
  notes: No AskUserQuestion usage. No interactive prompts.
         PascalCase tool names in tools: field may be deprecated; opencode uses lowercase internally.
-->
```

**Critical rules:**
- Do NOT place the comment inside the `---` delimiters (breaks YAML parsing per Pitfall 2 in RESEARCH.md)
- Do NOT modify any frontmatter fields
- Do NOT modify any content after the compat block
- Do NOT claim "PascalCase auto-maps" — state it "may be deprecated" per Pitfall 3 in RESEARCH.md
- Keep field names consistent across both files: `runtime:`, `status:`, `degraded:`, `notes:` (per Pitfall 4)
  </action>
  <verify>
    <automated>cd E:/Proyectos/pr-review-agent && grep -c "<!-- runtime-compat" agents/pr-reviewer.md agents/pr-fixer.md && grep -n "^---$" agents/pr-reviewer.md | head -2 && grep -n "runtime-compat" agents/pr-reviewer.md && grep -n "^---$" agents/pr-fixer.md | head -2 && grep -n "runtime-compat" agents/pr-fixer.md</automated>
  </verify>
  <acceptance_criteria>
- `agents/pr-reviewer.md` contains exactly one `<!-- runtime-compat` block
- `agents/pr-fixer.md` contains exactly one `<!-- runtime-compat` block
- In pr-reviewer.md: the `<!-- runtime-compat` line number is greater than the closing `---` line number and less than the `<role>` line number
- In pr-fixer.md: the `<!-- runtime-compat` line number is greater than the closing `---` line number and less than the `<role>` line number
- Both files contain `runtime: claude-code` and `runtime: opencode`
- Both files contain `status: full` (for claude-code) and `status: partial` (for opencode)
- Neither file contains the string "auto-map" or "auto-maps"
- pr-reviewer.md compat block contains `AskUserQuestion not invoked`
- pr-fixer.md compat block contains `No AskUserQuestion usage`
- YAML frontmatter in both files is unchanged (first line is `---`, `tools:` line matches original)
  </acceptance_criteria>
  <done>Both agent files have compat blocks correctly placed after frontmatter, documenting full Claude Code support and partial OpenCode support with specific degradation notes.</done>
</task>

<task type="auto">
  <name>Task 2: Add compat blocks to command files</name>
  <files>commands/pr-review/review.md, commands/pr-review/fix.md, commands/pr-review/setup.md</files>
  <read_first>commands/pr-review/review.md, commands/pr-review/fix.md, commands/pr-review/setup.md, .planning/phases/11-opencode-compatibility/11-RESEARCH.md</read_first>
  <action>
Insert an HTML comment compat block in each command file per D-01 and D-02.

**Placement (D-02):** After the closing `---` of YAML frontmatter, add one blank line, then the comment block, then one blank line before the existing `<objective>` tag. The `---` line, blank line, comment block, blank line, `<objective>` must appear in that exact order.

**commands/pr-review/review.md** — Insert between line 13 (`---`) and line 15 (`<objective>`):

```
<!-- runtime-compat
runtime: claude-code
  status: full
  notes: allowed-tools enforced, argument-hint displayed in TUI, AskUserQuestion available.
runtime: opencode
  status: partial
  degraded: allowed-tools ignored, argument-hint not displayed, AskUserQuestion not available
  notes: agent: delegation works. Tool permissions via opencode.json instead.
         AskUserQuestion listed in allowed-tools but not invoked by the agent; bash readline used.
-->
```

**commands/pr-review/fix.md** — Insert between line 14 (`---`) and line 16 (`<objective>`):

```
<!-- runtime-compat
runtime: claude-code
  status: full
  notes: allowed-tools enforced, argument-hint displayed in TUI, AskUserQuestion available.
runtime: opencode
  status: partial
  degraded: allowed-tools ignored, argument-hint not displayed, AskUserQuestion not available
  notes: agent: delegation works. Tool permissions via opencode.json instead.
         AskUserQuestion listed in allowed-tools but not invoked by the agent.
-->
```

**commands/pr-review/setup.md** — Insert between line 11 (`---`) and line 13 (`<objective>`):

```
<!-- runtime-compat
runtime: claude-code
  status: full
  notes: allowed-tools enforced, AskUserQuestion available.
runtime: opencode
  status: partial
  degraded: allowed-tools ignored, AskUserQuestion not available
  notes: No agent: field — runs inline. Tool permissions via opencode.json instead.
         AskUserQuestion listed in allowed-tools but setup uses bash readline if needed.
-->
```

**Critical rules:**
- Do NOT place the comment inside the `---` delimiters
- Do NOT modify any frontmatter fields (especially `allowed-tools` arrays and `argument-hint` values)
- Do NOT modify any content after the compat block
- Keep field names consistent: `runtime:`, `status:`, `degraded:`, `notes:`
- review.md and fix.md have `agent:` field; setup.md does NOT have `agent:` field — compat blocks reflect this difference
  </action>
  <verify>
    <automated>cd E:/Proyectos/pr-review-agent && grep -c "<!-- runtime-compat" commands/pr-review/review.md commands/pr-review/fix.md commands/pr-review/setup.md && grep -n "runtime-compat" commands/pr-review/review.md && grep -n "runtime-compat" commands/pr-review/fix.md && grep -n "runtime-compat" commands/pr-review/setup.md</automated>
  </verify>
  <acceptance_criteria>
- `commands/pr-review/review.md` contains exactly one `<!-- runtime-compat` block
- `commands/pr-review/fix.md` contains exactly one `<!-- runtime-compat` block
- `commands/pr-review/setup.md` contains exactly one `<!-- runtime-compat` block
- In all three files: the `<!-- runtime-compat` line number is greater than the closing `---` line and less than the `<objective>` line
- All three files contain `runtime: claude-code` and `runtime: opencode`
- All three files contain `status: full` (claude-code) and `status: partial` (opencode)
- All three files contain `allowed-tools ignored` in the opencode degraded field
- review.md compat block contains `argument-hint not displayed`
- fix.md compat block contains `argument-hint not displayed`
- setup.md compat block contains `No agent: field`
- YAML frontmatter in all three files is unchanged (allowed-tools arrays intact, argument-hint values intact)
  </acceptance_criteria>
  <done>All three command files have compat blocks correctly placed after frontmatter, documenting allowed-tools and argument-hint degradation on OpenCode, and clarifying AskUserQuestion is listed but not invoked.</done>
</task>

</tasks>

<verification>
After both tasks complete, verify all 5 files:

1. All 5 files contain exactly one `<!-- runtime-compat` block each
2. No compat block appears inside YAML frontmatter delimiters
3. Every compat block has both `runtime: claude-code` and `runtime: opencode` entries
4. No file contains "auto-map" or "auto-maps" (per Pitfall 3)
5. Field names are consistent across all 5 blocks: `runtime:`, `status:`, `degraded:`, `notes:`
6. YAML frontmatter is unchanged in all files (tools, allowed-tools, argument-hint, agent fields intact)

Automated check:
```bash
cd E:/Proyectos/pr-review-agent && for f in agents/pr-reviewer.md agents/pr-fixer.md commands/pr-review/review.md commands/pr-review/fix.md commands/pr-review/setup.md; do echo "=== $f ===" && grep -c "<!-- runtime-compat" "$f" && grep -c "auto-map" "$f"; done
```
Expected: Each file shows count 1 for runtime-compat and count 0 for auto-map.
</verification>

<success_criteria>
- All 5 agent/command files have inline runtime-compat HTML comment blocks (RTCOMPAT-02)
- Compat blocks document tool name status, frontmatter field behavior, and AskUserQuestion status per runtime (RTCOMPAT-02)
- Tool names confirmed PascalCase throughout with no changes needed (RTCOMPAT-01)
- Frontmatter fields documented as working/ignored per runtime (RTCOMPAT-01)
- No file has broken YAML frontmatter after the edit
</success_criteria>

<output>
After completion, create `.planning/phases/11-opencode-compatibility/11-01-SUMMARY.md`
</output>
